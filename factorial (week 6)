#include <stdio.h>

int f(int n) {
    return n < 2 ? 1 : n * f(n - 1);
}

int main() {
    int num;
    printf("Enter a natural number: ");
    scanf("%d", &num);
    printf("Factorial of %d is %d\n", num, f(num));
    return 0;
}